# Braid — frontend

The Next.js frontend for Braid. See the [project README](../README.md) for setup, environment variables, and deployment instructions.

```bash
npm install
npm run dev
```

Runs at [http://localhost:3000](http://localhost:3000).
